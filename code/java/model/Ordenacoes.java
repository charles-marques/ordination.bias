public class Ordenacoes {
	public static void main(String args[]) {
		"order by".compareTo();
		"order by".comparing
		"order by".order();
		"order by".orderBy();
		"order by".sort();
		"order by".sortBy{};
		"order by".sortBy {};
		"order by".sortBy();
		"order by".sortWith();
		"order by".sorted();
	}
	public void compare() {}
	public void Sort.by() {}
	public void OrderBy() {}
}
