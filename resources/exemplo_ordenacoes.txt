
ordenação em java/android:
.sort(
.compareTo(
public int compare
.comparing
.order(
.orderBy(

ordenação em java/kotlin:
.sortBy{
.sortBy {

ordenação em java/scala:
.sortBy(
.sortWith(
.sorted

ordenação Sprint
Sort.by(
OrderBy

ordenação em banco sql:
'order by'

